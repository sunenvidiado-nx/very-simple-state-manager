name: very_simple_state_manager
description: Very Simple State Manager is a lightweight Flutter state solution - it's in the name itself!
version: 1.0.0+1
repository: https://github.com/sunenvidiado-nx/very-simple-state-manager

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.0.0
